import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const cart = () => {
  return (
    <View>
      <ScrollView>
        
      </ScrollView>
    </View>
  )
}

export default cart